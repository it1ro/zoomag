<!-- File: Zoomag/Views/LoginWindow.xaml -->
<Window x:Class="Zoomag.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="350"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Логин" />
        <TextBox x:Name="LoginTextBox" Grid.Row="1" />
        <TextBlock Grid.Row="2" Text="Пароль" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" />
        <TextBlock Grid.Row="4" Text="Роль" />
        <ComboBox x:Name="RoleComboBox" Grid.Row="5" SelectedIndex="0">
            <!-- Заполняется в коде -->
        </ComboBox>
        <Button x:Name="LoginButton" Grid.Row="6" Content="Войти" Click="LoginButton_Click"/>
    </Grid>
</Window>