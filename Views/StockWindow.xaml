<Window x:Class="Zoomag.Views.StockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Склад" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Товары на складе"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"
                   FontSize="18" />

        <DataGrid x:Name="StockDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="120" />
                <DataGridTextColumn Header="Ед. изм." Binding="{Binding UnitName}" Width="80" />
                <DataGridTextColumn Header="Цена, ₽" Binding="{Binding Price}" Width="100" />
                <DataGridTextColumn Header="Остаток" Binding="{Binding Qty}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2"
                Content="Назад"
                HorizontalAlignment="Right"
                Padding="20,5"
                Click="GoBack_Click" />
    </Grid>
</Window>