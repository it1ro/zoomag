<Window x:Class="Zoomag.Views.Reports.IncomingGoodsJournalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Журнал поступления товаров" Height="450" Width="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="GoodsReceiptsGrid"
                  AutoGenerateColumns="False"
                  Grid.Column="0"
                  Grid.Row="0"
                  Grid.ColumnSpan="4"
                  FontSize="15"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата"
                                    Binding="{Binding SupplyDate, StringFormat=dd.MM.yyyy}"
                                    Width="100" />
                <DataGridTextColumn Header="Наименование"
                                    Binding="{Binding ProductName}"
                                    Width="250" />
                <DataGridTextColumn Header="Количество"
                                    Binding="{Binding Quantity, StringFormat=N0}"
                                    Width="100" />
                <DataGridTextColumn Header="Цена, ₽"
                                    Binding="{Binding Price, StringFormat=N2}"
                                    Width="100" />
                <DataGridTextColumn Header="Сумма, ₽"
                                    Binding="{Binding Total, StringFormat=N2}"
                                    Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <Button x:Name="ExportToExcelButton"
                Content="Вывод в Excel"
                FontSize="16"
                Grid.Row="1"
                Grid.Column="4"
                Margin="10"
                Padding="10,5"
                Click="ExportToExcel" />

        <Button x:Name="BackButton"
                Content="Назад"
                FontSize="16"
                Grid.Row="2"
                Grid.Column="4"
                Margin="10"
                Padding="10,5"
                Click="BackButton_Click" />
    </Grid>
</Window>