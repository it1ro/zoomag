# Zoomag — курсовая работа по разработке приложения учёта товаров

## Описание

Приложение для учёта товаров, складских остатков и генерации отчётов (например, нулевых остатков, поставок,
категорий).  
Реализовано на C# (.NET) с использованием WPF (интерфейс) и Entity Framework Core (база данных).

---

## 🚀 Как запустить проект

### 1. Получи исходники

- **С GitHub (или другого репо):**
  ```bash
  git clone https://github.com/твой-логин/Zoomag.git
  cd Zoomag
  ```

- **С флешки:**  
  Скопируй папку `Zoomag` на компьютер.

---

### 2. Открой в Visual Studio

- Запусти **Visual Studio 2022** (или новее).
- Перейди в меню:  
  `Файл` → `Открыть` → `Проект/Решение`
- Выбери файл:  
  **`Zoomag.sln`**

---

### 3. Восстанови пакеты и запусти миграции

> 💡 Нужно, чтобы в системе был установлен **SQL Server LocalDB** (по умолчанию идёт с VS).

Внутри Visual Studio открой **Package Manager Console**:  
`Вид` → `Другие окна` → `Package Manager Console`

Выполни по очереди:

```powershell
Update-Database
```

> ✅ Если появится сообщение `Done.` — миграции применены. База создана.

---

### 4. Запусти приложение

- Нажми `Ctrl + F5` или кнопку **"Запустить без отладки"** (зелёная стрелка).
- Откроется окно входа — логин и пароль не требуются (демо-режим).

---

## 📁 Структура проекта

```
Zoomag/
├── Models/          # Сущности базы данных (Product, Category и др.)
├── Data/            # DbContext и миграции EF Core
├── Services/        # Генератор отчётов (ZeroStockReportGenerator)
├── Views/           # Окна WPF (Login, ProductEditor, Reports и т.д.)
├── App.xaml         # Главный файл приложения
├── Zoomag.sln    # Решение Visual Studio
├── Zoomag.csproj # Проект
└── Призыв.xlsx      # Пример данных для импорта (не используется в коде)
```

---

## ⚠️ Возможные проблемы

- **Ошибка при `Update-Database`?**  
  Убедись, что:
    - В `AppDbContext.cs` строка подключения — `Server=(localdb)\\mssqllocaldb;Database=Zoomag;Trusted_Connection=true;`
    - У тебя установлен **SQL Server LocalDB** (идёт с Visual Studio).

- **Не открывается `.xlsx`?**  
  Это просто пример данных — приложение его **не читает**. Можно игнорировать.

- **Не компилируется?**  
  Попробуй: `Сборка` → `Восстановить пакеты NuGet`.

---

## ✅ Что проверить перед защитой

- [ ] Приложение запускается
- [ ] Окно входа открывается
- [ ] Можно открыть “Обзор товаров”
- [ ] Есть отчёты (например, “Нулевые остатки”)
- [ ] База данных создана в LocalDB (можно проверить в **SQL Server Object Explorer**)

---

**Автор:** Ильмир  
**Группа:** [указать]  
**Преподаватель:** [указать]

```