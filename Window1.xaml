<Window x:Class="prriva_10.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:prriva_10"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Grid.RowSpan="3" Name="dg" AutoGenerateColumns="False" ItemsSource="{Binding Test}" FontSize="24">
            <DataGrid.Columns>
                <DataGridTextColumn Header="дата" Width="200" Binding="{Binding Path=data}"></DataGridTextColumn>
                <DataGridTextColumn Header="наименование" Width="300" Binding="{Binding Path=naim}"></DataGridTextColumn>
                <DataGridTextColumn Header="ед/изм" Width="200" Binding="{Binding Path=ed}"></DataGridTextColumn>
                <DataGridTextColumn Header="кол__во" Width="100" Binding="{Binding Path=kol_vo}"></DataGridTextColumn>
                <DataGridTextColumn Header="цена" Width="200" Binding="{Binding Path=cena}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Column="0" Grid.Row="3" Content="Ввод списком" FontSize="25" Click="Button_Click" ></Button>
        <Button Grid.Column="1" Grid.Row="3"  Content="Запись в БД" FontSize="25" Click="Button_Click_1" ></Button>
        <Button Grid.Row="2" Grid.Column="2" Content="Назад" FontSize="24" Grid.RowSpan="2" Click="Button_Click_2"></Button>
    </Grid>
</Window>
